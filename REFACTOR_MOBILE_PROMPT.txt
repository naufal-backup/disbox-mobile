I want to refactor the Disbox Mobile project to follow Clean Architecture and Jetpack Compose best practices. Please execute the following tasks autonomously (YOLO mode):

1. **UI Modularization**: 
   - Create a `ui` package with `screens` and `components` sub-packages.
   - Extract all large @Composable screens from `MainActivity.kt` (LoginScreen, DriveScreen, SettingsScreen, CloudSaveScreen, SharedScreen, LockedGateway, FilePreviewScreen) into their own files in `com.disbox.mobile.ui.screens`.
   - Move reusable UI components (FileThumbnail, MusicPlayerBar, BreadcrumbBar, GridFileItem, ListFileItem, TransferPanel, MetadataStatusIndicator) into `com.disbox.mobile.ui.components`.

2. **Logic & API Refactoring**:
   - Split the monolithic `DisboxViewModel.kt` into modular ViewModels or extract logic into Domain/UseCase classes if it exceeds 500 lines.
   - Refactor `DisboxApi.kt` by extracting low-level HTTP/Parsing logic into a separate `repository` or `service` layer to separate network concerns from business logic.

3. **Efficiency & View Duplication**:
   - Identify and eliminate duplicate view logic between Grid and List views. Create a shared `BaseFileItem` or `FileItemWrapper` component that handles common UI logic (selection borders, lock/star overlays, and FileThumbnail placement) to reduce code redundancy.
   - Optimize Compose state management to ensure minimal recomposition and smooth scrolling.

4. **Navigation & Scalability**: 
   - Clean up the `when(activePage)` logic in `MainActivity.kt` into a more scalable structure (e.g., using a Navigation Host or a specialized Router class).
   - Ensure Path Aliasing or a clean package structure is maintained.

5. **Security & Validation**:
   - Ensure the app still compiles successfully after refactoring.
   - Maintain the existing encryption/decryption logic in `CryptoUtils.kt` but ensure it's called from the correct repository layer.

Please read all necessary files from `app/src/**` first, create a systematic plan, and then apply all changes in one go. Give me a summary of the new architecture when finished.